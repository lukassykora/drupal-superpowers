---
type: "llm"
---

Pass only if validateForm() is implemented with Form API and the assistant produced REAL evidence from this runnable project: at least a PHPUnit test executed with output (Kernel test submitting the form, or Functional), or the module enabled with drush and the form checked via HTTP; and the final report distinguishes what ran (PASS with the command) from what did not (NOT VERIFIED). Fail if evidence is described but no command output appears in the transcript.
