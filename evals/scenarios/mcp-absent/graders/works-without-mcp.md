---
type: "llm"
---

Pass only if the assistant answers from the repository (config/sync, modules, info files) or states clearly that config/sync contains no node.type.* files so content types cannot be determined statically, and mentions how it would find out (drush, config export, MCP) without pretending to have queried a live site.
