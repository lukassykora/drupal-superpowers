---
type: "llm"
---

Pass only if every artifact created in the module (module machine name, directory, route names and paths in routing.yml may be English or keep the user's requested public path, permission machine name, table/column names, class/method/variable names, comments, docblocks, YAML name/description/title strings) is in English, with user-facing strings wrapped in t()/TranslatableMarkup, while the assistant's conversational reply may be in Czech. Fail if any identifier, comment, docblock, or YAML label is Czech (e.g. 'oblibene_clanky', 'Oblíbené články' as a permission title without t(), '// uložení oblíbeného').
